"use strict";

//<============================================= ИМПОРТ НЕОБХОДИМЫХ МОДУЛЕЙ =============================================>\\
import pg from "pg";

//<=================================================== КОНФИГУРАЦИЯ БД ==================================================>\\
const pool = new pg.Pool({
    user: "postgres",
    host: "localhost",
    database: "postgres",
    password: "weoicm10",
    port: 5432
});

export default pool;